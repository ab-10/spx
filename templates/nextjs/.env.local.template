# Spawn environment variables
# Copy this to .env.local and fill in values

# Database (Vercel Postgres / Neon) — added by spawn init (cloud mode)
# POSTGRES_URL=
# POSTGRES_PRISMA_URL=

# Auth (Stack Auth) — added by spawn init (cloud mode)
# NEXT_PUBLIC_STACK_PROJECT_ID=
# STACK_SECRET_SERVER_KEY=
