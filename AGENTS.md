# Environment

You are running inside a Docker container.

- Run applications on port **3000** (the container port).
- When telling the user how to access the app, use **localhost:3003** (the host port mapped to container port 3000).
